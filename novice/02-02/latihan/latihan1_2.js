// unnamed
let Rectangle = class {
    constructor(height, width) {
      this.height = height;
      this.width = width;
    }
  };
  console.log(Rectangle.name);
  
  // named
  let Circle = class Circle {
    constructor(height, width) {
      this.height = height;
      this.width = width;
    }
  };
  console.log(Circle.name);